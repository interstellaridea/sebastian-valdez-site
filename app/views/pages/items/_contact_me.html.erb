	<div id="contact-me" class="row">
  <div class="container">
    <div class="row">
      <div class="col-7 mx-auto">
      <h2>Contact me</h2> 
      <%= form_with model: @message, url: contact_me_path, id: 'new_message' do |f| %>
	      
        <div class="form-group">
          <%= f.text_field :name, id: 'message_name', placeholder: 'Your Name', class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.text_field :subject, placeholder: 'Subject', class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.text_field :sender_email, placeholder: 'Your Email',class: 'form-control' %>
        </div>

        <div class="form-group">
          <%= f.text_area :body, placeholder: 'Your Message', class: 'form-control', rows: 5 %>
        </div>
          <%= f.button fa_icon('paper-plane',text: 'Send'), type: 'submit', class: 'btn btn-info' %>
          <%= button_tag 'Reset', type: 'reset', class: 'btn btn-secondary' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
